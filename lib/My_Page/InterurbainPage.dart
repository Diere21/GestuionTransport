import 'package:flutter/material.dart';
class InterurbainPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Transport interurbain'),
      ),
      body: Center(
        child: Text('Contenu de la page de transport interurbain'),
      ),
    );
  }
}