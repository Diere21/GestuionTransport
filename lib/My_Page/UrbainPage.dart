import 'package:flutter/material.dart';
class UrbainPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Transport urbain'),
      ),
      body: Center(
        child: Text('Contenu de la page de transport urbain'),
      ),
    );
  }
}
